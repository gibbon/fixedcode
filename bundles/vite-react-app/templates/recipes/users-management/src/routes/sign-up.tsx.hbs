// Generated by fixedcode (recipe: users-management) — extension point.
// Customize the styling / branding here. Re-running generate will not overwrite.

import { useState } from 'react';
import type { CSSProperties, FormEvent } from 'react';
import { useAuth } from '../auth/useAuth';

const AFTER_SIGN_IN_PATH = '{{usersManagement.afterSignInPath}}';
const SIGN_IN_PATH = '{{usersManagement.signInPath}}';

const wrapStyle: CSSProperties = {
  maxWidth: 360,
  margin: '64px auto',
  padding: 24,
  border: '1px solid #e0e0e6',
  borderRadius: 8,
  fontFamily: 'system-ui, -apple-system, sans-serif',
};
const fieldStyle: CSSProperties = { display: 'block', width: '100%', padding: 8, marginBottom: 12, boxSizing: 'border-box' };
const labelStyle: CSSProperties = { display: 'block', marginBottom: 4, fontSize: 13, color: '#444' };
const buttonStyle: CSSProperties = { width: '100%', padding: '10px 12px', cursor: 'pointer' };
const errorStyle: CSSProperties = { color: '#b00020', marginBottom: 12, fontSize: 13 };
const headingStyle: CSSProperties = { marginTop: 0 };
const footerTextStyle: CSSProperties = { fontSize: 13, marginTop: 16 };

export default function SignUpPage() {
  const { signUp } = useAuth();
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [submitting, setSubmitting] = useState(false);
  const [error, setError] = useState<string | null>(null);

  async function onSubmit(e: FormEvent<HTMLFormElement>) {
    e.preventDefault();
    setError(null);
    if (password.length < 8) {
      setError('Password must be at least 8 characters.');
      return;
    }
    setSubmitting(true);
    try {
      await signUp({ email, password });
      window.location.assign(AFTER_SIGN_IN_PATH);
    } catch (err) {
      setError(err instanceof Error ? err.message : 'Sign up failed');
    } finally {
      setSubmitting(false);
    }
  }

  return (
    <div style={wrapStyle}>
      <h1 style={headingStyle}>Create account</h1>
      <form onSubmit={onSubmit}>
        <label style={labelStyle} htmlFor="email">Email</label>
        <input
          id="email"
          type="email"
          required
          autoComplete="email"
          value={email}
          onChange={(e) => setEmail(e.target.value)}
          style={fieldStyle}
        />
        <label style={labelStyle} htmlFor="password">Password (min 8 chars)</label>
        <input
          id="password"
          type="password"
          required
          minLength={8}
          autoComplete="new-password"
          value={password}
          onChange={(e) => setPassword(e.target.value)}
          style={fieldStyle}
        />
        {error ? <div style={errorStyle}>{error}</div> : null}
        <button type="submit" disabled={submitting} style={buttonStyle}>
          {submitting ? 'Creating...' : 'Sign up'}
        </button>
      </form>
      <p style={footerTextStyle}>
        Already registered? <a href={SIGN_IN_PATH}>Sign in</a>
      </p>
    </div>
  );
}
