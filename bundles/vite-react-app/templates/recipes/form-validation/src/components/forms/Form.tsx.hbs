// Generated by fixedcode (recipe: form-validation) — extension point.
// Form wrapper that wires react-hook-form's submit handler to a typed callback.
// Customise markup freely; the props contract is stable.

import type { ReactNode } from 'react';
import type { FieldValues, UseFormReturn } from 'react-hook-form';

export interface FormProps<TValues extends FieldValues> {
  form: UseFormReturn<TValues>;
  onSubmit: (values: TValues) => void | Promise<void>;
  /** Server-side error to surface above the field-level errors. */
  rootError?: string | null;
  className?: string;
  children: ReactNode;
}

export default function Form<TValues extends FieldValues>({
  form,
  onSubmit,
  rootError,
  className,
  children,
}: FormProps<TValues>) {
  return (
    <form
      noValidate
      className={className ?? 'flex flex-col gap-4'}
      onSubmit={form.handleSubmit(onSubmit)}
    >
      {rootError ? (
        <div
          role="alert"
          className="rounded border border-red-200 bg-red-50 px-3 py-2 text-sm text-red-700"
        >
          {rootError}
        </div>
      ) : null}
      {children}
    </form>
  );
}
