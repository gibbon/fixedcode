// Generated by fixedcode (recipe: dashboard) — extension point.
// Single stat tile. Customise the markup; props contract is stable.

import { formatValue, type StatFormat } from '../lib/dashboard';

export interface StatCardProps {
  name: string;
  value: number | null;
  units?: string;
  format: StatFormat;
  loading?: boolean;
}

export default function StatCard({
  name,
  value,
  units,
  format,
  loading = false,
}: StatCardProps) {
  return (
    <article
      className="flex flex-col justify-between rounded-lg border border-neutral-200 bg-white p-5"
      data-stat={name}
    >
      <header>
        <h3 className="text-xs font-medium uppercase tracking-wide text-neutral-500">{name}</h3>
      </header>

      {loading || value === null ? (
        <div className="mt-3 h-9 w-24 animate-pulse rounded bg-neutral-100" aria-hidden />
      ) : (
        <div className="mt-3 flex items-baseline gap-1.5">
          <span className="text-3xl font-semibold tabular-nums tracking-tight text-neutral-900">
            {formatValue(value, format)}
          </span>
          {units ? <span className="text-sm text-neutral-500">{units}</span> : null}
        </div>
      )}
    </article>
  );
}
