// Generated by fixedcode (recipe: admin-screen) — extension point.
// Sidebar shell for the admin section. Customize the chrome here.

import type { CSSProperties, ReactNode } from 'react';

export interface AdminLayoutProps {
  /** The current page (rendered to the right of the sidebar). */
  children: ReactNode;
}

const SIDEBAR_LINKS = [
{{#each adminScreen.aggregates}}  { label: '{{namePluralPascal}}', href: '{{../adminScreen.basePath}}/{{namePluralKebab}}' },
{{/each}}];

const rootStyle: CSSProperties = { display: 'flex', minHeight: '100vh' };
const sidebarStyle: CSSProperties = {
  width: 220,
  background: '#f5f5f7',
  borderRight: '1px solid #e0e0e6',
  padding: '16px 12px',
};
const titleStyle: CSSProperties = { fontSize: 18, marginTop: 0 };
const navListStyle: CSSProperties = {
  listStyle: 'none',
  padding: 0,
  margin: 0,
  display: 'grid',
  gap: 4,
};
const linkStyle: CSSProperties = {
  display: 'block',
  padding: '6px 8px',
  borderRadius: 4,
  color: '#222',
  textDecoration: 'none',
};
const mainStyle: CSSProperties = { flex: 1, padding: 24 };

export default function AdminLayout({ children }: AdminLayoutProps) {
  return (
    <div style={rootStyle}>
      <aside style={sidebarStyle}>
        <h1 style={titleStyle}>Admin</h1>
        <nav>
          <ul style={navListStyle}>
            {SIDEBAR_LINKS.map((l) => (
              <li key={l.href}>
                <a href={l.href} style={linkStyle}>
                  {l.label}
                </a>
              </li>
            ))}
          </ul>
        </nav>
      </aside>
      <main style={mainStyle}>{children}</main>
    </div>
  );
}
