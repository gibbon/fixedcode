// Generated by fixedcode (recipe: admin-screen) — extension point.
// Customize how Create looks/behaves for {{agg.namePascal}}. Re-running generate will not overwrite it.
//
// Navigation uses window.location to stay router-agnostic. If you wired the
// admin shell into TanStack Router or React Router, swap this for the router's
// navigate() — see admin/index.ts for the route table.

import {{agg.namePascal}}Form from './{{agg.namePascal}}Form';
import { create{{agg.namePascal}} } from './{{agg.namePascal}}Client';
import type { {{agg.namePascal}}Input } from './types';

export default function {{agg.namePascal}}CreatePage() {
  const handleSubmit = async (input: {{agg.namePascal}}Input) => {
    await create{{agg.namePascal}}(input);
    window.location.assign('{{adminScreen.basePath}}/{{agg.namePluralKebab}}');
  };

  return (
    <section>
      <h2>New {{agg.namePascal}}</h2>
      <{{agg.namePascal}}Form onSubmit={handleSubmit} submitLabel="Create" />
    </section>
  );
}
