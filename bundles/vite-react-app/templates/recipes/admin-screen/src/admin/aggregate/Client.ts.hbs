// Generated by fixedcode (recipe: admin-screen). Do not edit by hand — re-run generate.
// Typed CRUD client for the {{agg.namePascal}} aggregate. Uses native fetch + JSON.

import type { {{agg.namePascal}}, {{agg.namePascal}}Input, {{agg.namePascal}}Page } from './types';

const RAW_BASE = (import.meta.env.{{apiUrlEnvVar}} ?? '{{adminScreen.apiBaseUrl}}').replace(/\/+$/, '');
const API_PATH = '/api/{{agg.apiPath}}';

function endpoint(suffix = ''): string {
  return `${RAW_BASE}${API_PATH}${suffix}`;
}

async function request<T>(input: RequestInfo, init?: RequestInit): Promise<T> {
  const res = await fetch(input, {
    ...init,
    headers: {
      'Content-Type': 'application/json',
      ...(init?.headers ?? {}),
    },
  });
  if (!res.ok) {
    const text = await res.text().catch(() => '');
    throw new Error(`{{agg.namePascal}} ${init?.method ?? 'GET'} failed: ${res.status} ${res.statusText}${text ? ` — ${text}` : ''}`);
  }
  if (res.status === 204) return undefined as unknown as T;
  return (await res.json()) as T;
}

export interface List{{agg.namePluralPascal}}Params {
  page?: number;
  size?: number;
  q?: string;
}

export async function list{{agg.namePluralPascal}}(params: List{{agg.namePluralPascal}}Params = {}): Promise<{{agg.namePascal}}Page> {
  const qs = new URLSearchParams();
  if (params.page !== undefined) qs.set('page', String(params.page));
  if (params.size !== undefined) qs.set('size', String(params.size));
  if (params.q) qs.set('q', params.q);
  const suffix = qs.toString() ? `?${qs.toString()}` : '';
  return request<{{agg.namePascal}}Page>(endpoint(suffix));
}

export async function get{{agg.namePascal}}(id: string): Promise<{{agg.namePascal}}> {
  return request<{{agg.namePascal}}>(endpoint(`/${encodeURIComponent(id)}`));
}

export async function create{{agg.namePascal}}(input: {{agg.namePascal}}Input): Promise<{{agg.namePascal}}> {
  return request<{{agg.namePascal}}>(endpoint(), {
    method: 'POST',
    body: JSON.stringify(input),
  });
}

export async function update{{agg.namePascal}}(id: string, input: {{agg.namePascal}}Input): Promise<{{agg.namePascal}}> {
  return request<{{agg.namePascal}}>(endpoint(`/${encodeURIComponent(id)}`), {
    method: 'PUT',
    body: JSON.stringify(input),
  });
}

export async function delete{{agg.namePascal}}(id: string): Promise<void> {
  await request<void>(endpoint(`/${encodeURIComponent(id)}`), { method: 'DELETE' });
}
