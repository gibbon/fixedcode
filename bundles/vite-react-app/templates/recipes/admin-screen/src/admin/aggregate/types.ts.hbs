// Generated by fixedcode (recipe: admin-screen). Do not edit by hand — re-run generate.
// Mirrors the {{agg.namePascal}} aggregate from your spring-domain spec.

export interface {{agg.namePascal}} {
{{#each agg.fields}}  {{name}}{{#unless required}}?{{/unless}}: {{#if (eq formInputType "number")}}number{{else}}{{#if (eq formInputType "checkbox")}}boolean{{else}}string{{/if}}{{/if}};
{{/each}}
}

/**
 * Shape sent on Create/Update — the identity field is omitted on create
 * (the backend assigns it) and supplied via the URL on update.
 */
export interface {{agg.namePascal}}Input {
{{#each agg.inputFields}}  {{name}}{{#unless required}}?{{/unless}}: {{#if (eq formInputType "number")}}number{{else}}{{#if (eq formInputType "checkbox")}}boolean{{else}}string{{/if}}{{/if}};
{{/each}}
}

export interface {{agg.namePascal}}Page {
  content: {{agg.namePascal}}[];
  page: number;
  size: number;
  totalElements: number;
  totalPages: number;
}
