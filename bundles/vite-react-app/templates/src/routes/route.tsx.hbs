// Extension point: implement the {{route.name}} page below.
// This file is generated once and then user-owned (overwrite: false in the manifest).

export default function {{route.componentName}}() {
  return (
    <section>
      <h2>{{route.name}}</h2>
      <p>TODO: implement {{route.name}} ({{route.path}}).</p>
    </section>
  );
}
