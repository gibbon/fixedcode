package {{packageName}}.auth

import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder
import org.springframework.stereotype.Component

/**
 * Generated by fixedcode (recipe: users-management) — extension point.
 *
 * Default: BCrypt at strength 12. Swap to Argon2 / scrypt by reimplementing
 * the two methods below; nothing else in the recipe depends on the impl.
 */
@Component
class PasswordHasher {
    private val encoder = BCryptPasswordEncoder(12)

    fun hash(rawPassword: String): String = encoder.encode(rawPassword)

    fun verify(rawPassword: String, hash: String): Boolean = encoder.matches(rawPassword, hash)
}
