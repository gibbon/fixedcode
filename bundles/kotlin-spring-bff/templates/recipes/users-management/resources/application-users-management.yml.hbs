# Generated by fixedcode (recipe: users-management).
# Activated automatically by application.yml when the recipe is enabled.
app:
  auth:
    jwtSecret: {{springProp "JWT_SECRET" "change-me-in-prod-min-32-chars-please"}}
    tokenTtlMinutes: {{usersManagement.tokenTtlMinutes}}
    # Seeded only when the `dev` profile is active. Production should bootstrap
    # the first admin via a one-off SQL or migration. See V001__users.sql.
    defaultAdminEmail: {{springProp "DEFAULT_ADMIN_EMAIL" usersManagement.defaultAdminEmail}}

---
spring:
  config:
    activate:
      on-profile: dev
app:
  auth:
    seedDefaultAdmin: true
