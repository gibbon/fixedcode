package {{packageName}}.pagination

import org.springframework.boot.context.properties.ConfigurationProperties

/**
 * Generated by fixedcode (recipe: pagination-filter-sort).
 *
 * Bound from `app.pagination.*` in `application-pagination-filter-sort.yml`.
 * Tune defaults globally without re-generating: set `APP_PAGINATION_DEFAULT_PAGE_SIZE`
 * or `APP_PAGINATION_MAX_PAGE_SIZE` env vars.
 */
@ConfigurationProperties(prefix = "app.pagination")
data class PaginationProperties(
    val defaultPageSize: Int = {{paginationFilterSort.defaultPageSize}},
    val maxPageSize: Int = {{paginationFilterSort.maxPageSize}},
)
