# Generated by fixedcode (recipe: audit-log).
# Activated automatically by application.yml when the recipe is enabled.
app:
  audit:
{{#if auditLog.hasActorHeader}}
    actorHeader: {{springProp "APP_AUDIT_ACTOR_HEADER" auditLog.actorHeader}}
{{else}}
    # Header-based actor resolution disabled (auditLog.actorHeader is empty).
{{/if}}
