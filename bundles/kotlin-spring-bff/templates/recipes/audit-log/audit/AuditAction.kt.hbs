package {{packageName}}.audit

/**
 * Generated by fixedcode (recipe: audit-log).
 *
 * Closed set of mutations the audit log captures. Add new variants here only
 * if your domain genuinely needs a new category — the FE filter and any
 * downstream SIEM/Kafka consumers usually pivot off this enum.
 */
enum class AuditAction {
    CREATED,
    UPDATED,
    DELETED,
}
