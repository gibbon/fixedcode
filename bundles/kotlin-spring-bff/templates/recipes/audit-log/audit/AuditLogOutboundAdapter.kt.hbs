package {{packageName}}.audit

import org.springframework.stereotype.Component

/**
 * Generated by fixedcode (recipe: audit-log) — extension point.
 *
 * Forwards a persisted [AuditLog] entry to wherever you want it to land beyond
 * the local DB — Kafka, SQS, Datadog, Splunk, Snowflake, etc. The default impl
 * is a no-op; replace the class body to wire your sink.
 *
 * Called synchronously from [AuditLogEventListener] *after* the audit row has
 * been committed to Postgres. If your sink is high-latency, post to a queue
 * here and forward asynchronously rather than blocking the request thread.
 */
@Component
class AuditLogOutboundAdapter {
    fun forward(entry: AuditLog) {
        // No-op default. Wire your downstream sink here, e.g.:
        //
        //   kafkaTemplate.send("audit-log", entry.id.toString(), entry)
        //
        // or:
        //
        //   sqsClient.sendMessage(SendMessageRequest.builder()
        //       .queueUrl(queueUrl)
        //       .messageBody(objectMapper.writeValueAsString(entry))
        //       .build())
    }
}
