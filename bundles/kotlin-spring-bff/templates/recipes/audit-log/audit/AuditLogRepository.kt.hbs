package {{packageName}}.audit

import org.springframework.data.domain.Page
import org.springframework.data.domain.Pageable
import org.springframework.data.jpa.repository.JpaRepository
import org.springframework.stereotype.Repository
import java.util.UUID

/**
 * Generated by fixedcode (recipe: audit-log).
 *
 * Spring Data JPA repository over the audit_log table. Pre-baked finders
 * cover the common admin lookups; add domain-specific queries here freely.
 */
@Repository
interface AuditLogRepository : JpaRepository<AuditLog, UUID> {
    fun findByAggregateTypeAndAggregateIdOrderByOccurredAtDesc(
        aggregateType: String,
        aggregateId: String,
        pageable: Pageable,
    ): Page<AuditLog>

    fun findByAggregateTypeOrderByOccurredAtDesc(
        aggregateType: String,
        pageable: Pageable,
    ): Page<AuditLog>

    fun findByActorOrderByOccurredAtDesc(actor: String, pageable: Pageable): Page<AuditLog>
}
