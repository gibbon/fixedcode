# {{appName.pascal}}

Kotlin/Spring Boot Backend-For-Frontend service.
Generated by [fixedcode](https://fixedcode.ai) — bundle `kotlin-spring-bff`.

- Java toolchain: {{javaVersion}}
- HTTP port: {{port}}
- Auth: `{{features.auth}}`
- Database: `{{features.database}}`
- Cache: `{{features.cache}}`
- Docker: `{{features.docker}}`

## Run

```bash
./gradlew bootRun
# or with the local profile
SPRING_PROFILES_ACTIVE=local ./gradlew bootRun
```

Health: `curl http://localhost:{{port}}/actuator/health`
OpenAPI UI: `http://localhost:{{port}}/swagger-ui.html`

## Composed downstream services

{{#if hasServices}}
| Name | Default base URL | Env override |
|------|------------------|--------------|
{{#each services}}
| `{{name}}` | `{{baseUrl}}` | `{{envVar}}` |
{{/each}}

A typed client (`{{appName.pascal}}` → `clients/<Name>Client.kt`) is generated for each service, wrapped in a Resilience4j `@CircuitBreaker(name = "default")`.
{{else}}
None declared in the spec. Add entries under `spec.services` to scaffold typed clients.
{{/if}}

## Regenerating

This project was generated from a YAML spec. Re-run `fixedcode generate <spec>.yaml -o .` to refresh.
Generated files are tracked in `.fixedcode-manifest.json`. Files marked `overwrite: false` are user-owned and never replaced.
