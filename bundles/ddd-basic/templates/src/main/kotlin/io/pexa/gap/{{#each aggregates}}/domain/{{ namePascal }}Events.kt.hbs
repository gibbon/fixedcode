package io.pexa.gap.{{ nameKebab }}.domain;

import java.time.LocalDateTime;
import java.util.UUID;

{{#each events}}
public class {{ namePascal }} {
    private final UUID aggregateId;
    private final LocalDateTime timestamp;

    {{#each fields}}
    private {{ javaType }} {{ nameCamel }};
    {{/each}}

    public {{ namePascal }}(UUID aggregateId) {
        this.aggregateId = aggregateId;
        this.timestamp = LocalDateTime.now();
    }

    public UUID getAggregateId() {
        return aggregateId;
    }

    public LocalDateTime getTimestamp() {
        return timestamp;
    }

    {{#each fields}}
    public {{ javaType }} get{{ namePascal }}() {
        return {{ nameCamel }};
    }

    public void set{{ namePascal }}({{ javaType }} {{ nameCamel }}) {
        this.{{ nameCamel }} = {{ nameCamel }};
    }
    {{/each}}
}

{{/each}}