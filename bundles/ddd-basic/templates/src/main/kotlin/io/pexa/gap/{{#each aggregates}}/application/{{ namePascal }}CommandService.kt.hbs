package io.pexa.gap.{{ nameKebab }}.application;

import io.pexa.gap.{{ nameKebab }}.domain.{{ namePascal }};
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;
import java.util.Optional;

@Service
@Transactional
public class {{ namePascal }}CommandService {

    public {{ namePascal }} create({{ namePascal }} entity) {
        // TODO: Implement command handler
        return entity;
    }

    public {{ namePascal }} update({{ namePascal }} entity) {
        // TODO: Implement command handler
        return entity;
    }

    {{#each commands}}
    public {{ emitPascal }} handle{{ namePascal }}({{ namePascal }}Command{{ namePascal }} cmd) {
        // TODO: Implement {{ name }}
        {{ namePascal }} entity = new {{ namePascal }}();
        {{#each params}}
        entity.set{{ namePascal }}(cmd.get{{ namePascal }}());
        {{/each}}
        return new {{ emitPascal }}();
    }
    {{/each}}
}