# {{ NameKebab }} MCP Server

{{ Description }}

PIDP-deployable wrapper around `{{ Image }}`.

## Quick Start

```bash
docker compose up
```

The MCP server will be available at `http://localhost:{{ Port }}/mcp`.
Health check: `http://localhost:{{ Port }}/health`.

{{#if HealthPath }}
## Authentication

Requests require the `X-API-Key` header. Set the `MCP_API_KEY` environment variable with the expected key.
{{/if}}

## Environment Variables

| Variable | Description |
|----------|-------------|
{{#each Env}}| `{{ @key }}` | {{ this }} |
{{/each}}
## Generated by GAP

This service was generated from a YAML spec using `fixedcode generate --spec <spec>.yaml`.