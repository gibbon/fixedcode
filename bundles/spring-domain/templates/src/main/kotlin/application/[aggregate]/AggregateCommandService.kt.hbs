package {{packageDot}}.application.{{names.kebab}}

{{#each commands}}
import {{../packageDot}}.domain.{{../names.kebab}}.commands.{{names.pascal}}Command
{{/each}}

interface {{names.pascal}}CommandService {
{{#each commands}}
    fun handle(command: {{names.pascal}}Command)
{{/each}}
}
