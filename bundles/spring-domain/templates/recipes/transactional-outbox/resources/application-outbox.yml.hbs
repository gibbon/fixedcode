# Generated by fixedcode (recipe: transactional-outbox).
# Optional — paste this block into your app's main application.yml or include
# this file as a Spring profile (`spring.profiles.include: outbox`). The
# OutboxProperties class also carries the same defaults at compile time, so
# the recipe works without this file when the values match.
app:
  outbox:
    pollIntervalMs: {{springProp "APP_OUTBOX_POLL_INTERVAL_MS" outbox.pollIntervalMs}}
    batchSize: {{springProp "APP_OUTBOX_BATCH_SIZE" outbox.batchSize}}
    maxAttempts: {{springProp "APP_OUTBOX_MAX_ATTEMPTS" outbox.maxAttempts}}
